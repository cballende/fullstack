Hola Seba! Ejercicio 1:
 SQL-base de datos-Esquema: El diagrama es correcto, representando la estructura lógica de la base de datos de manera visual.
 Es una herramienta esencial en el diseño de bases de datos porque ayuda a comprender cómo se organizan los datos y cómo se 
 relacionan entre sí antes de implementarlos en el sistema de gestión de bases de datos. Creaste correctamente la base de datos.
 Observacion: - Los nombres de los campos tienen etiquetas PK  y FK, lo cual no es válido en SQL, te tira error, eliminalas de
 los nombres de los campos quedando asi: nro_customer INT NOT NULL PRIMARY KEY. -Ponele AUTO_INCREMENT en las claves primarias
 numéricas para evitar ingresar manualmente los valores. Ej: nro_customer INT NOT NULL AUTO_INCREMENT PRIMARY KEY.-.
 Aprobado con observaciones.

SQL-DML: Tus consultas estan bien realizadas, solo tengo observaciones en los siguientes: 
Ejercicio 10: aca hay un error, pq la condición AND tipo='F' se aplica a todo el filtro, lo que no cumple exactamente con la 
	consigna que pide un OR en el tipo.Correccion: SELECT * FROM E01_TELEFONO WHERE (codigo_area<>844 AND nro_telefono>4369984) OR tipo='F';.
Ejercicio 12: aca hay un error, pq tu query solo va a funcionar si la columna fecha es un VARCHAR, pero si es DATE,
	el formato esperado es YYYY-MM-DD. Aprobado.

 Ejercicio 3-MongoDB-NoSQL: Tus consultas estan bien realizadas, con observaciones en los siguientes:
 Ejercicio 2: tu query excluye 50 y 150, debería ser: {stock: {$gte: 50, $lte: 150}}.
 Ejercicio 8: la query esta bien y funciona, podrias ponerla asi para optimizar: {precio: {$nin: [25, 100]}}.
 
Buen trabajo!. Aprobado. Tambien me gusto como tenes organizado tu github! (edited) 

Consulta: 

	Hola Seba querés referenciar un campo de una colección con otro de otra colección en MongoDB usando $ref y 
	validar eso con un validador tipo jsonSchema, es asi verdad? MongoDB no permite validar que un campo referencie 
	a un documento de otra colección usando $ref dentro de jsonSchema. Deberias guardar la referencia manualmente, como un ObjectId:
	por ejemplo: {
	  "cliente_id": ObjectId("6629f2c9adf454bde0fae83a")
	}       Validar la forma del campo en el validador:  validator: {
	  $jsonSchema: {
	    bsonType: "object",
	    required: ["cliente_id"],
	    properties: {
	      cliente_id: {
	        bsonType: "objectId",
	        description: "Debe ser un ObjectId válido"
	      }
	    }
	  }
	}
	2:39
	validator: {
	  $jsonSchema: {
	    bsonType: "object",
	    required: ["cliente_id"],
	    properties: {
	      cliente_id: {
	        bsonType: "objectId",
	        description: "Debe ser un ObjectId válido"
	      }
	    }
	  }
	}
	2:40
MongoDB no implementa validaciones entre colecciones. No hay foreign keys. La consistencia referencial se maneja desde la aplicación,
 no desde el motor de base de datos.


Hola Seba! Ejercicio 4:  Miniproyecto-Sist. de reservas vuelos-MongoDB:
 
	El script está bien dividido por secciones: selección de base de datos, inserción de datos y relaciones.
	Utilizas los métodos correctos para insertar múltiples o un solo documento (insertMany e insertOne) .
	La relación entre pasajeros y vuelos a través de los IDs es un buen ejemplo de normalización en MongoDB,
	usando referencias en lugar de documentos embebidos. Bien implementado new Date() para guardar la fecha actual.
	Es una buena práctica para trazabilidad de registros.
	
	Observaciones:
	 - podrias evitar redundancia en inserción de reservas, hay dos bloques casi iguales para insertar
		 la reserva de Ana en el vuelo "VueloSi", podrias eliminar uno de los dos bloques para evitar duplicación innecesaria.
	 -Tambien, podrías unificar nombres de variables vuelo o vueloId para mantener consistencia.
	 -En la colección vuelos, usás campos en ingles, en cambio en pasajeros usás campos en español,
		elegí un único idioma para los nombres de los campos, preferentemente inglés si el proyecto apunta a un entorno más técnico .
	Aprobado

	Trabajaste solo? pq tengo entendido que era grupal

	La parte 1 con las respuestas teoricas esta muy bien, se nota comprensión real de los conceptos.


